# TDD開発フロー

このプロジェクトではテスト駆動開発（TDD）を採用する。以下のフローに従うこと。

## TDDの基本フロー

```
要件 → ケース分析（網羅的に洗い出し） → 分割（10個程度に） → テストケース提示 → 承認 → テスト作成 → Red → 実装 → Green → リファクタリング → 次の仕様
```

1. **要件整理** - 実装する機能の仕様・背景・制約条件を明確にする
2. **ケース分析** - 要件から発生しうるケースを網羅的に洗い出す（数を最初から制限しない）
3. **分割** - 洗い出したケースが多い場合、レビュー可能な単位（10個程度）に分割する
4. **テストケース提示** - 仕様に基づいてテストケースを一覧で提示する（コードは書かない）
5. **テストケース承認** - 人間がテストケースの妥当性・網羅性を確認し承認する
6. **テスト作成** - 承認されたテストケースをコードとして実装する
7. **テスト実行（Red）** - テストが失敗することを確認する
8. **実装（Green）** - テストが通る最小限の実装を行う
9. **テスト実行（確認）** - 全てのテストが通ることを確認する
10. **リファクタリング** - 必要に応じてコードを改善する（テストは変更しない）

## 重要なルール

### 1. テストケースの事前承認

- テストコードを書く前に、テストケース一覧を提示して承認を得る
- **承認なしにテストコードを書き始めない**
- **レビュアーが判断できる情報（機能概要・入出力仕様・ケース分析）を必ず提示する**
- テストケース提示時は以下のフォーマットを使用する：

```markdown
### 📋 テストケース提案: {機能名}

#### 1. 機能概要
この機能が何をするか、なぜ必要かを簡潔に説明

#### 2. 前提条件
- この関数が呼ばれる文脈・タイミング
- 事前に満たされているべき条件
- 依存する外部サービスやデータの状態

#### 3. 入力・出力の仕様
- 入力: どのようなデータが渡されるか
- 出力: どのようなデータを返すか
- 副作用: 外部APIの呼び出し等

#### 4. 発生しうるケースの分析
| ケース | 入力例 | 期待される結果 | 備考 |
|--------|--------|----------------|------|
| 正常系 | ... | ... | ... |
| エラー系 | ... | ... | ... |
| 境界値 | ... | ... | ... |

#### 5. テストケース一覧
上記のケース分析に基づいて導出されたテストケース

**正常系**
1. テストケース名 - 何をテストするか
2. ...

**異常系**
1. テストケース名 - 何をテストするか
2. ...

**エッジケース**
1. テストケース名 - 何をテストするか
2. ...
```

### 2. テストコードの書き換え禁止

- テストを通すためにテストコードを修正することは禁止
- テストが間違っている場合は、仕様を再確認してから修正する
- テストの修正が必要な場合は、明確な理由を説明する

### 3. テストケースの洗い出しとレビュー単位

- まず要件からテストケースを網羅的に洗い出す（数を最初から制限しない）
- **1回のレビュー単位は10個程度を目安とする**（レビュー可能な規模）
- 要件から洗い出したテストケースが10個を超える場合は、仕様を分割して複数回のレビューに分ける
- 1つの関数は単一責任で小さく保つ
- 大きな機能は複数の小さい関数に分割する

### 4. 段階的な進行

- 1つの関数の実装・テスト・レビューが完了してから次の仕様に進む
- 各ステップでレビュアーが変更を追いやすいようにする
- レビュー → 承認 → 次の仕様のサイクルを守る

## テストの命名規則

```typescript
describe('対象の機能やモジュール名', () => {
  describe('正常系', () => {
    it('〜の場合、〜となる', () => { ... });
  });
  describe('異常系', () => {
    it('〜の場合、エラーとなる', () => { ... });
  });
  describe('エッジケース', () => {
    it('〜の場合でも正しく処理される', () => { ... });
  });
});
```
